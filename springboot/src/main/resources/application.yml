server:
  port : 9090
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springtest?serverTimezone=GMT%2b8
    username: root
    password: 123456
mybatis:
  mapper-locations: classpath:mapper/*.xml #????mybatis?xml??
#  configuration:（此配置换成mybatis-plus进行配置
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl(日志中显示sql)
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl